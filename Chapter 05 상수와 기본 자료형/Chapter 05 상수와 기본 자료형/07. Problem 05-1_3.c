/*
문제 3
질문 1) 아스키 코드를 구성하는 값의 범위는 어떻게 되는가? => 0이상 127이하
	책 답안 => 값의 범위는 0이상 127이하이다. 따라서 1바이트로 표현 가능하다.
질문 2) 대문자 A에서부터 대문자 Z까지 값이 어떻게 증가하는가? 증가의 폭을 말해보자. => 1씩 증가. 65~90
	책 답안 => 값이 1씩 증가한다.
질문 3) 알파벳 대문자와 소문자간의 값의 차는 어떻게 되는가? 규칙성을 찾아보자. => 32차
	책 답안 => 문자 A는 65, 문자 a는 97이다. 그리고 대문자와 소문자 모두 증가의 폭은 1로 동일하다.
		   따라서 모든 대문자와 소문자간 값의 차는 32이다.
*/
